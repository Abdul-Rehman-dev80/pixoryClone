export default function HeaderNav() {
  return (
    <div className="text-center bg-amber-100 px-[30px] font-glitz py-[10px]">
      <h2>
        shipping to uae is temporarily disabled - while we work on a fix you can
        still create & save projects
      </h2>
    </div>
  );
}
